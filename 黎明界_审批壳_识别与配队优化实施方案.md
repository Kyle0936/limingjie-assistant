# 黎明界自动化：审批壳 + 有界识别 + 节点/角色/配队优化实施方案

> 目标：在不推倒现有黎明界自动化逻辑的前提下，为现有识别与动作系统增加一层“流程审批壳”，把系统从“看到什么就处理什么”改造成“只允许识别和执行当前流程中合理出现的内容”。
>
> 适用场景：无人干扰、游戏流程基本固定、页面切换存在动画/瞬时帧/奖励循环、当前主要痛点为误识别、识别速度慢、节点映射不稳定、角色识别易受头像框和滚动影响、配队质量不够稳定。

---

# 1. 为什么要做这一层

当前系统的主要问题，不是“完全识别不到”，而是：

1. 同一帧往往会运行太多无关识别器。
2. 页面识别结果直接驱动动作，缺少业务层审批。
3. 瞬时帧、动画残影、奖励中间页可能被错误理解为正式流程节点。
4. 节点识别在尝试“理解整张地图”，但程序实际上已经知道下一节点是谁。
5. 角色识别在不同页面重复做身份判断，没有充分利用布局和前序状态。
6. 配队逻辑更偏“局部高分”，没有充分利用整局资源和后续 Boss / EX 需求。
7. 识别失败后缺少统一恢复机制，容易无限等待或直接停住。

黎明界的真实流程并不是开放世界，而是：

```text
宏观流程近似线性
+
节点内部有限分支
+
奖励页面允许有限循环
```

因此最合适的设计不是继续扩大自由识别能力，而是缩小每个阶段的感知范围。

核心原则：

```text
我知道现在应该在哪
↓
我知道下一步允许出现什么
↓
只识别这些内容
↓
识别结果经过审批
↓
才允许执行动作
```

---

# 2. 总体目标架构

建议把现有系统拆成 4 层：

```text
┌────────────────────┐
│ Flow Controller    │
│ 当前处于什么流程阶段 │
└─────────┬──────────┘
          │ expected pages / actions
          ↓
┌────────────────────┐
│ Approval Guard     │
│ 页面是否合法        │
│ 动作是否合法        │
│ 时序是否合理        │
└─────────┬──────────┘
          │ scoped recognition request
          ↓
┌────────────────────┐
│ Scoped Recognizer  │
│ 只识别当前候选页面   │
│ 只扫描当前需要的 ROI │
└─────────┬──────────┘
          │ observation
          ↓
┌────────────────────┐
│ Action Executor    │
│ click / swipe      │
│ team / battle ops  │
└─────────┬──────────┘
          │
          ↓
       等待后续证据
          │
          ↓
        COMMIT
```

现有识别器可以保留，但职责需要变化：

```text
旧：
Recognizer -> Action

新：
Recognizer -> Observation -> Approval Guard -> Action
```

识别器只能报告“我看到了什么”，不能自己决定“所以现在点什么”。

---

# 3. 分支建议

建议新开分支：

```text
feature/dawn-approval-guard
```

第一阶段不要同时重构配队、节点识别、角色识别。

先建立流程壳和日志。

等壳稳定以后，再分阶段替换底层识别。

推荐拆成：

```text
Phase 1  审批壳 + 流程约束
Phase 2  ROI / Scoped Recognition
Phase 3  节点识别重构
Phase 4  角色识别重构
Phase 5  配队决策重构
Phase 6  Replay 回归系统
```

---

# 4. Phase 1：审批壳

## 4.1 新增核心概念

建议至少新增：

```text
FlowPhase
PageObservation
ActionProposal
ApprovalResult
TransitionContext
```

示例：

```kotlin
enum class FlowPhase {
    ENTRY_TITLE,
    ENTRY_LOADING,
    ENTRY_HOME,
    ENTRY_ADVENTURE,
    ENTRY_DAWN_HOME,
    ENTRY_INITIAL_PICK,
    ENTRY_INITIAL_REWARD,

    MAP_IDLE,
    MAP_NODE_CLICKED,
    MAP_MOVE_CONFIRM,

    NODE_CONTENT,
    NODE_REWARD_LOOP,

    RUN_FINAL_SETTLEMENT,

    RECOVERY,
    PAUSED
}
```

页面类型仍然保留：

```text
TITLE_WAITING_TAP
GAME_LOADING_PROGRESS
HOME
ADVENTURE
NODE_SELECTION
INITIAL_CHARACTER_SELECTION
CHARACTER_JOINED
...
```

但 PageType 不再直接等于业务状态。

例如：

```text
Page = RUN_CLEAR_RESULT
```

只代表：

```text
当前屏幕像最终分数页
```

而不是：

```text
整局已经 complete
```

---

# 5. 页面审批表

每个 FlowPhase 都维护：

```text
allowedPages
preferredPages
allowedActions
timeout
recoveryPolicy
```

例如：

```text
ENTRY_INITIAL_PICK

preferred:
- INITIAL_CHARACTER_SELECTION

allowed:
- INITIAL_CHARACTER_SELECTION
- GAME_LOADING_PROGRESS
- UNKNOWN

forbidden:
- NODE_SELECTION
- HOME
- SHOP
- BATTLE_*
```

如果此时识别到：

```text
NODE_SELECTION
```

则：

```text
recognition = true
approval = false
action = NONE
reason = ILLEGAL_TRANSITION
```

这会直接解决“出发后闪过几帧节点地图，程序提前接管路线”的问题。

---

# 6. 入口流程建议

入口不再作为大量平级页面，而是严格协议：

```text
ENTRY_TITLE
   ↓
ENTRY_LOADING
   ↓
ENTRY_HOME
   ↓
ENTRY_ADVENTURE
   ↓
ENTRY_DAWN_HOME
   ↓
ENTRY_INITIAL_PICK
   ↓
ENTRY_INITIAL_REWARD
   ↓
MAP_IDLE
```

---

## 6.1 ENTRY_TITLE

允许识别：

```text
TITLE_WAITING_TAP
GAME_LOADING_PROGRESS
HOME_ANNOUNCEMENT
HOME
```

允许动作：

```text
CLICK_START
```

降级动作：

```text
(950,950)
```

成功条件：

```text
页面已离开 TITLE_WAITING_TAP
```

---

## 6.2 ENTRY_LOADING

允许：

```text
GAME_LOADING_PROGRESS
PRE_HOME_DATA_LOADING
UNKNOWN
HOME_ANNOUNCEMENT
HOME
```

此阶段唯一允许的“脏点击”：

```text
(950,930)
```

约束：

```text
interval >= 900ms
maxClicks = 30
maxDuration = 45s
```

重要：

```text
只有已经进入 ENTRY_LOADING 后
UNKNOWN 才允许脏点击
```

禁止：

```text
应用启动后 UNKNOWN 直接乱点
```

---

## 6.3 ENTRY_INITIAL_PICK

用户人工选择三名角色。

程序只负责确认：

```text
selectedCount == 3
inviteButtonEnabled == true
```

然后：

```text
CLICK_INVITE
```

进入：

```text
ENTRY_INITIAL_REWARD
```

此时即使短暂检测到 NODE_SELECTION：

```text
IGNORE
```

---

## 6.4 ENTRY_INITIAL_REWARD

维护门禁：

```text
initialCharactersConfirmed = true
characterJoinedCloseRequested = false
```

允许：

```text
UNKNOWN
CHARACTER_JOINED
ITEM_REWARD
NODE_SELECTION
```

其中：

```text
NODE_SELECTION
```

只有：

```text
characterJoinedCloseRequested == true
```

以后才可以视为：

```text
ENTRY COMPLETE
```

否则：

```text
IGNORE TRANSIENT MAP FRAME
```

---

# 7. Phase 2：有界识别 Scoped Recognition

当前不要让所有 recognizer 每帧全部运行。

改成三级。

---

## 7.1 Fast Path

只运行当前最可能出现的 1~3 个识别器。

例如：

```text
FlowPhase = MAP_MOVE_CONFIRM
```

只检查：

```text
MOVE_CONFIRMATION
NODE_CONTENT_ENTRY
NODE_SELECTION
```

不要检查：

```text
HOME
ADVENTURE
SHOP_PURCHASE_COMPLETE
RUN_CLEAR_RESULT
...
```

---

## 7.2 Recovery Path

Fast Path 连续失败，例如：

```text
3 frames
or
800ms
```

扩展少量候选。

例如：

```text
Fast:
MOVE_CONFIRMATION

Recovery:
MOVE_CONFIRMATION
NODE_SELECTION
GAME_LOADING
BATTLE_CHALLENGE
LINK_CHOICE
...
```

仍然不全识别。

---

## 7.3 Diagnostic Scan

连续异常超过例如：

```text
5~10s
```

才运行一次全局诊断。

但全局诊断：

```text
只允许观察
禁止自动动作
```

即：

```text
diagnosticRecognized = HOME
```

不能马上：

```text
CLICK_ADVENTURE
```

必须交给 Recovery / Approval Guard 判断。

---

# 8. ROI 化识别

每个识别器声明自己的 ROI。

例如：

```text
BATTLE_IN_PROGRESS

ROI:
- timer
- autoButton
- instantUbButton
- speedButton
```

不要扫描整屏。

```text
SHOP

ROI:
- shopTitle
- itemSlots
- refreshButton
- closeButton
```

```text
NODE_SELECTION

ROI:
- mapViewport
```

这样做的原因：

1. 降低 NCC / OCR 搜索面积。
2. 避免页面其他区域产生脏候选。
3. 提高识别速度。
4. 更容易做页面级阈值调整。

---

# 9. Phase 3：节点识别重构

## 9.1 核心思想

现有逻辑倾向于：

```text
识别屏幕所有节点
↓
分类
↓
排序
↓
推断 blockId
↓
找到目标
```

改成：

```text
已知 currentNode
↓
已知 nextNode
↓
已知 nextNode 类型/逻辑列/视觉位置
↓
只寻找这个目标
```

视觉的职责从：

```text
理解整张地图
```

变成：

```text
将当前截图与已知地图对齐
```

---

# 10. 新增 MapTracker

建议新增：

```text
MapTracker
```

持有：

```text
viewportOffset
visibleColumns
lastVisibleNodes
scrollDirection
lastViewportSignature
leftBoundaryReached
rightBoundaryReached
```

流程：

```text
初次进入地图
↓
识别可见节点结构
↓
建立 viewport anchor
↓
以后每帧主要跟踪相对位移
```

滚动后：

```text
旧节点中心位置
vs
新节点中心位置
```

估算：

```text
dx
```

更新：

```text
viewportOffset += dx
```

这样不会每滑一下就从零重新理解整张地图。

---

# 11. 节点位置识别和节点类型识别分开

不要一个模板同时负责：

```text
这里有个节点
+
它是 EX
```

建议：

```text
Step 1
Node Detector
→ 找中心 / 外框

Step 2
Node Classifier
→ 对节点内部小 ROI 分类
```

类型：

```text
NORMAL
EX
EVENT
LINK
RELIC
SHOP
BOSS
```

这样高亮、选中框、地图动画只影响 Detector 或 Classifier 的其中一部分。

---

# 12. 利用地图拓扑做节点审批

最终节点得分不要只看视觉相似度。

建议：

```text
score =
visualScore
+ expectedTypePrior
+ expectedColumnPrior
+ expectedVerticalPrior
+ graphConsistency
+ temporalConsistency
```

如果路线告诉你：

```text
nextNode = EX#40601
```

那么即使屏幕里：

```text
EX candidate       visual = 0.76
SHOP candidate     visual = 0.91
```

也不能简单点击 SHOP。

因为拓扑约束表明：

```text
当前合法 successor = EX#40601
```

这类场景应优先：

```text
route prior > raw visual score
```

---

# 13. 节点滚动流程

旧：

```text
找不到
↓
滑
↓
重新全图识别
↓
找不到
↓
继续滑
```

新：

```text
目标当前不在 viewport
↓
根据 target logical column 判断滑动方向
↓
滑动一次
↓
更新 viewportOffset
↓
预测目标出现区域
↓
仅扫描预测区域
```

仍保留：

```text
maxScroll = 4
```

但边界判断不再只依赖“整个 viewport signature 完全相同”。

增加：

```text
node displacement
scrollbar displacement
viewport anchors
```

多证据判断是否真的滑不动。

---

# 14. Phase 4：角色识别重构

角色识别应从：

```text
全屏寻找某个角色模板
```

改成：

```text
找槽位
↓
裁角色内部区域
↓
归一化
↓
识别身份
```

---

# 15. Slot-first

不同页面拥有固定槽位结构。

例如：

### 三选一

```text
slot1
slot2
slot3
```

### 商店

```text
itemSlot1
itemSlot2
itemSlot3
```

### 编组

```text
5 columns × visibleRows
```

先定位 slot。

不要先问：

```text
胡桃在哪里？
```

而是：

```text
slot2 里是谁？
```

---

# 16. 头像归一化

每个 slot 裁出头像以后：

```text
1. 去除边框
2. 去除星级
3. 去除角标
4. 去除选中框
5. 裁到头像内部稳定区域
6. resize 到统一尺寸
```

然后再和角色库比较。

原因：

```text
同一个角色
在三选一 / 编组 / 商店
外框和缩放并不一致
```

真正稳定的是头像主体，而不是 UI 外框。

---

# 17. Top-K 而不是单结果

角色识别输出：

```text
slot2:
  胡桃            0.66
  胡桃(圣诞节)    0.62
  铃莓            0.18
```

不要立刻：

```text
slot2 = 胡桃
```

而是保留 Top-K。

然后使用全局约束求最优组合。

例如三个槽：

```text
slot A:
  角色1 0.82
  角色2 0.79

slot B:
  角色1 0.80
  角色3 0.76

slot C:
  角色4 0.91
```

独立 top1 会得到：

```text
角色1
角色1
角色4
```

非法。

全局匹配：

```text
角色2
角色1
角色4
```

---

# 18. 使用游戏规则辅助角色身份判断

可使用约束：

```text
当前本局已经拥有的角色
候选页角色不能重复
已经购买的角色
已经确认加入的角色
账号角色池
当前属性筛选
当前角色列表排序
```

例如：

```text
胡桃已经在 roster 中
```

若该奖励机制确定不会重复，则：

```text
P(胡桃) = 0
```

这样视觉分数不高时也可以利用上下文修正。

---

# 19. 角色身份缓存

只在首次出现时识别。

例如：

```text
三选一
↓
识别：
A / B / C
↓
点击 B
```

立刻记录：

```text
pendingAcquiredCharacter = B
```

后续：

```text
立绘
CHARACTER_JOINED
```

不再重新判断：

```text
加入的是谁？
```

CHARACTER_JOINED 只需要证明：

```text
获得流程已经进入结果页
```

同理：

```text
商店商品识别
↓
点击购买
↓
购买确认
↓
购买成功
```

都应继承：

```text
pendingPurchase.character
```

---

# 20. CharacterGridGeometry

统一角色滚动列表几何模型。

建议新增：

```text
CharacterGridGeometry
```

维护：

```text
columnCenters
rowHeight
gridTop
gridBottom
scrollOffset
firstVisibleRow
partialTopRow
partialBottomRow
```

这样不同功能不要各自重新推导角色列表位置。

---

# 21. 编组页面滚动

初次进入：

```text
检测 5 列中心
检测 rowHeight
检测首行基线
```

每次滚动后：

```text
通过头像整体位移
+
滚动条位移
```

估算：

```text
scrollOffset
```

因此：

```text
顶部只露半行
```

可以明确知道：

```text
这是上一行残留
```

而不是误判为完整新行。

这主要解决过去出现的：

```text
半宽
底部高度错误
第一行锁不到
滑到底部后识别逻辑变化
```

---

# 22. Phase 5：配队决策重构

配队不要直接依赖识别器。

拆成：

```text
Perception
↓
Game State
↓
Team Planner
```

---

# 23. GameState

至少维护：

```text
roster
currentHp
usedCharacters
currentRelics
attributeBonuses
physicalMagicBias
currentArea
nodeType
bossInfo
retryCount
```

视觉只负责更新 GameState。

Team Planner 不读取截图。

---

# 24. 单角色评分升级为边际价值

不要只计算：

```text
CharacterScore(character)
```

而是：

```text
MarginalValue(character | currentRoster)
```

例如：

```text
当前有 3 个法系输出
但没有奶
```

那么：

```text
再来一个 90 分法系输出
实际边际价值可能只有 +15
```

而：

```text
一个基础分 70 的奶
可能带来 +50
```

---

# 25. 统一用于三种决策

同一个 evaluator 同时服务：

```text
角色三选一
商店买角色
战斗编组
```

避免三个地方各自维护一套角色分数。

---

# 26. TeamScore

建议逐步加入：

```text
TeamScore =
Survival
+ Damage
+ AttributeSynergy
+ PhysicalMagicConsistency
+ SupportCompatibility
+ HealCoverage
+ TankCoverage
+ RelicSynergy
+ BossCounter
- ResourceWaste
- FutureOpportunityCost
```

其中：

```text
FutureOpportunityCost
```

非常重要。

普通战只需要安全通过。

不应把所有最强角色一次性塞进去。

---

# 27. 普通战和 Boss / EX 使用不同目标

普通战：

```text
寻找“最低成本可稳定通过队伍”
```

而不是：

```text
寻找理论最强队伍
```

Boss / EX：

```text
允许提高资源投入
```

---

# 28. 三队 Boss：联合规划

不要：

```text
先选最强第一队
↓
剩下的人组成第二队
↓
再剩下的组成第三队
```

否则很容易：

```text
第一队 150
第二队 90
第三队 35
```

建议先做角色资源分桶：

```text
Tank
Healer
Main DPS
Support
Universal Support
```

保证三队基础完整。

然后求：

```text
maximize min(team1Score, team2Score, team3Score)
```

或至少：

```text
避免任何一队低于安全阈值
```

---

# 29. 节点奖励状态机

节点奖励不要写成：

```text
EX -> RELIC -> CHARACTER -> MAP
```

应写成：

```text
NODE_CONTENT
↓
POST_REWARD_LOOP
↓
MAP
```

POST_REWARD_LOOP 可接受：

```text
CHARACTER_SELECTION
CHARACTER_JOINED
RELIC_CHOICE
ITEM_REWARD
EVENT_CHOICE
SHOP
...
```

循环直到：

```text
NODE_SELECTION
```

---

# 30. 为什么奖励必须允许循环

因为真实节点可能是：

```text
普通战
→ 两次下一步
→ 角色选择
→ 加入
→ 地图
```

也可能：

```text
EX
→ 战斗
→ 遗物
→ 角色
→ 道具
→ 地图
```

也可能：

```text
事件
→ 二选一
→ 动画
→ 嵌套事件
→ 遗物
→ 角色
→ 地图
```

因此：

```text
节点类型
```

不能硬编码唯一奖励链。

---

# 31. RUN_CLEAR_RESULT 修复

当前：

```text
RUN_CLEAR_RESULT
=> complete = true
```

应该改为：

```text
RUN_CLEAR_RESULT
=> FINAL_SCORE_SHOWN
```

执行：

```text
CLICK_CLOSE
```

然后进入：

```text
RUN_FINAL_SETTLEMENT
```

允许：

```text
CHEST
ITEM_REWARD
CHARACTER_REWARD
...
```

直到真正出现：

```text
DAWN_REALM_HOME
```

或一个明确的：

```text
RUN_SETTLEMENT_FINISHED
```

再：

```text
routeProgress.complete = true
```

---

# 32. 动作审批票

所有自动动作都经过统一校验：

```text
pageAllowed
flowPhaseAllowed
actionAllowed
confidenceEnough
stableFramesEnough
targetInsideExpectedROI
cooldownPassed
retryBudgetAvailable
frameFresh
```

例如：

```text
[APPROVAL]

phase=MAP_IDLE
page=NODE_SELECTION
expectedNode=EX#40601
candidate=EX
confidence=0.91
stableFrames=4
roiValid=true
cooldown=true
action=CLICK_NODE
approved=true
```

拒绝时：

```text
[APPROVAL DENIED]

phase=ENTRY_INITIAL_PICK
recognized=NODE_SELECTION
reason=ILLEGAL_TRANSITION
action=NONE
```

---

# 33. Recovery 机制

不要再出现两种极端：

```text
识别错了继续乱点
```

或：

```text
识别失败直接无限等待
```

统一：

```text
Fast Path
↓ fail
Recovery Path
↓ fail
Diagnostic Scan
↓ fail
PAUSED / MANUAL
```

---

# 34. 推荐异常策略

## 短暂非法页面

例如：

```text
1~2 frames NODE_SELECTION
```

处理：

```text
IGNORE
```

---

## 合理旁路页面

例如预期：

```text
CHARACTER_JOINED
```

却出现：

```text
ITEM_REWARD
```

且 ITEM_REWARD 在当前 reward protocol 合法。

处理：

```text
ACCEPT RECOVERY TRANSITION
```

---

## 持续非法页面

例如遗物奖励阶段连续看到：

```text
HOME
```

处理：

```text
重新截图
↓
Recovery Recognizers
↓
Diagnostic Scan
↓
若仍确认 HOME
PAUSE
```

禁止：

```text
自动点“冒险”
```

---

# 35. Phase 6：Replay 回归测试

这一项建议尽早预留数据结构。

每次自动化运行保存：

```text
timestamp
frame
flowPhase
expectedPages
recognitionCandidates
selectedObservation
actionProposal
approvalResult
targetROI
confidence
routeState
rosterState
```

---

# 36. 离线 Replay

实现一个：

```text
ReplaySession
```

读取历史帧重新执行：

```text
recognizer
approval
state transition
```

但禁止真实点击。

这样以后修改识别算法，不需要重复打游戏。

---

# 37. 推荐统计指标

每次 replay 输出：

```text
totalFrames
unknownFrames
illegalTransitions
falsePositivePages
nodeMisses
characterUncertainCount
recoveryCount
pauseCount
avgRecognitionMs
p95RecognitionMs
```

例如：

```text
Old:
UNKNOWN          47
illegal page      6
wrong character   3
avg recognize   184ms

New:
UNKNOWN          11
illegal page      0
wrong character   1
avg recognize    61ms
```

只有这种对比才能知道优化到底有没有用。

---

# 38. 推荐实际执行顺序

## Step 1

只加：

```text
FlowPhase
Approval Guard
日志
```

不改任何识别算法。

验收：

```text
现有流程仍然能跑
非法状态不会触发动作
```

---

## Step 2

给入口流程加入：

```text
expectedPages
```

重点修：

```text
首次 NODE_SELECTION 闪帧
UNKNOWN 脏点击范围
角色加入后才允许交接地图
```

验收：

```text
入口完整跑完
不提前进入路线状态机
```

---

## Step 3

给路线流程加入：

```text
MAP_IDLE
MAP_NODE_CLICKED
MAP_MOVE_CONFIRM
NODE_CONTENT
NODE_REWARD_LOOP
```

验收：

```text
节点点击只有确认页面后才 commit
奖励页可以任意合法循环
```

---

## Step 4

改成 Scoped Recognition。

先只改入口和节点路线。

验收：

```text
识别耗时明显下降
CPU 占用下降
行为不变
```

---

## Step 5

引入 MapTracker。

不要立刻删除旧节点识别。

先：

```text
新旧算法同时运行
```

只让旧算法控制动作。

对比日志：

```text
oldTarget
newTarget
```

一致率稳定后再切换。

---

## Step 6

重构角色识别。

顺序：

```text
slot detector
↓
头像内部裁切
↓
Top-K
↓
全局匹配
↓
角色缓存
```

仍建议和旧算法并跑一段时间。

---

## Step 7

统一 CharacterGridGeometry。

重点解决：

```text
顶部半行
底部半行
滚动到底
第一行定位
筛选后列表重排
```

---

## Step 8

最后才改配队。

因为：

```text
识别输入不稳定
```

时优化 Team Planner 没意义。

---

# 39. 不建议第一版同时动的东西

第一版审批壳阶段先不要：

```text
彻底重写模板系统
彻底重写路线算法
一次性换成 OCR / AI Vision
重做全部配队
重写战斗状态识别
```

否则很难判断：

```text
到底是架构问题
还是识别问题
还是策略问题
```

---

# 40. 第一版最小验收目标

如果 feature/dawn-approval-guard 第一版能做到以下几点，就已经值得合并：

```text
1. 非法页面绝不会自动触发动作

2. 入口闪过 NODE_SELECTION 不会提前进入路线状态机

3. UNKNOWN 只有在明确允许的阶段才能进行受限点击

4. 节点点击后必须看到合法节点内容页才 commit

5. 奖励页面允许循环直到回到地图

6. RUN_CLEAR_RESULT 不再被视为真正结束

7. 每次自动动作都能在日志中看到审批原因

8. 识别器可以按当前 FlowPhase 缩小候选范围
```

---

# 41. 第二版验收目标

```text
1. 节点只围绕 nextNode 定向寻找

2. MapTracker 能维持 viewport

3. 滚动后不再每次从零重新理解地图

4. 角色识别以 slot 为单位

5. Top-K + 不重复约束生效

6. 已知角色身份不跨页面重复识别

7. CharacterGridGeometry 统一角色列表坐标
```

---

# 42. 第三版验收目标

```text
1. 三选一角色使用边际价值

2. 商店购买与角色选择共用 evaluator

3. 普通战优先最小资源可过队伍

4. EX / Boss 可以保留关键角色

5. 三队 Boss 不再第一队吃掉全部核心资源
```

---

# 43. 最终设计原则

整个系统以后遵守以下规则：

```text
视觉不是流程控制器。
```

```text
PageType 不是业务状态。
```

```text
识别到某页面，不代表允许处理该页面。
```

```text
点击成功，不代表逻辑迁移成功。
```

```text
只有后续页面证据成立，才 commit。
```

```text
已知事实不重复识别。
```

```text
能用流程先验解决的问题，不交给视觉猜。
```

```text
能用 ROI 解决的问题，不扫描整屏。
```

```text
能用地图拓扑解决的问题，不只看模板分数。
```

```text
能用角色槽位和 roster 约束解决的问题，不只看头像相似度。
```

```text
配队目标不是“这一场最强”，而是“整局资源分配最优”。
```

---

# 44. 一句话总结

这次分支的重点不是：

```text
让识别器变得更聪明
```

而是：

```text
让识别器没有机会在不该做决定的时候做决定。
```

然后再逐步把节点识别、角色识别和配队替换成更有先验、更小搜索空间、更可验证的实现。
