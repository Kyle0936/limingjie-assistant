# 参考来源与洁净实现边界

## 原则

- 参考旧项目的可观察输入、输出、错误语义和测试结果。
- Kotlin 侧重新定义模型、接口和实现，不机械逐行翻译 Python。
- 不提交真实账号、密码、Token、会话密钥或未脱敏网络报文。
- 仅提交项目自身实现；开发者的本机参考仓库和素材不随项目发布。

## 当前参考

### AutoPCR

- 上游：<https://github.com/cc004/autopcr>
- 用途：国服登录、游戏协议行为、黎明界地图与刷开局行为参考。
- 最终产物：不包含 Python、Chaquopy 或 AutoPCR 本机 HTTP 服务。

### AutoPCR Android 本地参考

- 用途：已有 Android UI、账号配置和原生迁移过程参考。
- 状态：本机参考副本不进入 Git 提交。

### 可可萝自动会战助手

- 用途：会战领域逻辑、纯 Kotlin 视觉识别、MediaProjection、无障碍服务和测试资源。
- 所有权：项目所有者自有项目，可直接迁移和重构。

### LandosolRoster 角色名称表

- 上游：<https://github.com/Ice9Coffee/LandosolRoster>
- 文件：`_pcr_data.py` 中的 `CHARA_NAME`、`UnavailableChara`。
- 许可证：GPL-3.0（2026-07-22 通过 GitHub 仓库 API 确认）。
- 用途：仅提取角色 ID、中文/日文/英文名称、别名和可用性；不提取或推断黎明界职能、攻击类型、评分等决策数据。
- 本机源快照不随仓库发布；进入 APK 的 JSON 通过可重复的离线转换生成，并保留本文件与第三方声明。

### 国服角色与技能数据

- 上游：<https://github.com/SonderXiaoming/priconne-database>。
- 用途：核对国服 `unit_id`、角色变体与技能表的主键关系；完整 SQLite 数据库不打包进 APK，避免把服务端数据库作为运行时依赖。
- 2026-08-18 校验到的国服数据库版本为 `202608171854`；黎明界队伍选择仍以资源包中的角色 ID 为主键，后续评分/技能权重可在此基础上增量导入。
- 角色正式名称通过 `tools/import_cn_official_names.py` 从该数据库的 `unit_data` 导入到角色资源 `officialName`；未收录的未来角色保留 LandosolRoster 名称作为回退。

### 国服黎明界视觉素材

- 来源：项目所有者提供的截图裁剪和 BMP 模板；原始截图不随仓库发布。
- 用途：主页、黎明界入口、出发、公会选择和战斗确认的识别锚点；当前只做资源完整性校验，不直接触发点击。
- 进入 APK 前统一重命名为 ASCII 路径，并在 `vision.json` 记录尺寸和 SHA-256；原始截图目录继续保持本地隔离。

### 共识迷宫数据表

- 来源：项目使用的黎明界迷宫数据工作簿；原始工作簿不随仓库发布。
- 用途：迷宫遗物、事件、印记、环境效果、功能性角色、任务、累计奖励、α强化、结算数据和更新记录。
- 导入方式：`tools/import_labyrinth_workbook.py` 直接读取 XLSX XML，避免 Tencent Office 样式表兼容问题。
- 明确排除：`EXTREME`、`BOSS`、`攻略`、`EX首饰` 图片型工作表不进入 APK；其中攻略表完全不作为应用资源。
- 遗物图标：只通过 XLSX `richData` 关系提取“迷宫遗物”工作表 B 列的 109 张内嵌 PNG，按遗物 ID 重命名并记录 SHA-256。

### 角色图标素材

- 上游：<https://redive.estertion.win/icon/unit/>；本机缓存不进入 Git。
- 导入边界：文件名必须符合 `icon_unit_<角色ID><变体>.png` 或 `.webp`，且角色 ID 已存在于角色目录；无法对应的 7 张图片不绑定到任何角色。
- 完整性：导入后的 801 张图标均记录实际格式、尺寸和 SHA-256；其中包含 WebP 内容的资源保留为 `.webp` 扩展名。
- 权利边界：游戏素材的相关权利归原权利人；正式公开发布前需单独复核素材分发范围。

### 国服官方客户端与在线端点

- 客户端：模拟器已安装的 `com.bilibili.priconne`，版本与签名只用于互操作校验。
- 渠道服客户端：小米渠道 `com.bilibili.priconne.mi`（雷电模拟器实测）。渠道服使用独立游戏服网关 `l1-prod-uo-gs-gzlj.bilibiligame.net` 与对应 RES-KEY / PLATFORM-ID，取值对照 AutoPCR 的 `sdk/sdkclients.py`（`qsdkclient`）；账号页填写的 uid / access_key 直通游戏服登录，不经过 Bilibili SDK 登录。
- 在线端点：Bilibili 游戏 SDK 的 RSA、登录和验证码初始化接口，以及游戏服公开发现、维护、登录握手和只读账号资料接口。
- 用途：验证当前字段结构、MessagePack/AES-CBC 封包、会话头、错误码和人工验证码流程，不保存真实请求或响应。
- 验证码：运行时加载 Geetest 官方组件，应用不包含验证码破解或第三方打码实现。
