# 商店商品类别确认（2026-09-14）

日志 `limingjie-debug-20260914-013529.zip` 中第三格标题被读成“增益型识能的随机印记”。旧逻辑强制包含“职能”，且只对未确认遗物执行标题 OCR，最终在疑似遗物分数约 0.715、候选分差仅 0.000338 的情况下持续等待。

现在所有商品先经过标题类别确认，明确的印记标题会清除遗物候选。完整的已知职能印记标题允许局部 OCR 单字容错；不会从印记共享图标猜测具体职能。遗物图标匹配门槛保持不变。

同一标题画面最多使用三种读取方式：原裁剪放大两倍、放大三倍、扩大上下边界后放大三倍。每轮等待窗口为三秒，每种方式最多提交一次。确认成功的标题按像素指纹缓存，标题变化或离开商店后失效；调度器仍拒绝旧画面回调。

未解决商品标为 `EXHAUSTED`，不购买、不计入遗物售罄；继续处理可确认商品，没有可购买项时退出。确认了本轮三个遗物已购完后仍遵循原区域刷新策略。诊断 JSON 新增 `categoryState` 和 `categoryAttempt`，保留 OCR 原文、证据 ID 和遗物分差。

回归保存日志中的原始游戏帧及商品状态，使用应用完整遗物图标库与日志实际 OCR 文本验证三印记分类。另覆盖高分遗物误匹配、标题容错边界、有限重试、旧回调、商品变更和刷新条件。离线回归不等同于 Android OCR 引擎及完整实机流程验证。
