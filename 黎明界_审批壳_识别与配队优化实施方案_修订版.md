# 黎明界自动化优化实施方案（修订版）

> 本文是《黎明界_审批壳_识别与配队优化实施方案》对照 HEAD 代码核实后的修订版本。原方案保留不动。
>
> 修订依据：`android/app/src/main/java/com/landosol/toolbox/labyrinth/` 全量代码审计，以及 `log/` 下 6 个调试包合并 3173 帧的实测耗时。
>
> 结论：原方案诊断的问题基本成立，但对现状的判断有约六成已经过时，其中三条是事实错误。真正的性能瓶颈只有一处，而原方案把它排在第三顺位。

---

# 第一部分：对原方案的三处事实修正

## 修正一：最终分数页已经不会直接标记完成

原方案第 31 节认为现状是 `RUN_CLEAR_RESULT => complete = true`，需要改造成多阶段结算。

**这不是现状。** 代码里已有一条六阶段结算链，由 `LabyrinthPostBossStage` 驱动（`LabyrinthEntryRecognitionSession.kt:564-571`）：

```text
NONE -> BEFORE_SCORE -> SCORE_RESULT -> CHEST_SEQUENCE
     -> FINAL_ITEM_REWARD -> WAITING_FOR_DAWN_HOME
```

只有回到黎明界主页才置完成标志（`markRunCleared`，`:4741-4752`），并带 20 秒超时保护（`FINAL_HOME_RETURN_TIMEOUT_MILLIS`，`:7029`），超时则停机且不标完成。

原方案「第一版验收目标」第 6 条已经满足，本项从方案中移除。

**但审计发现一条真实的未覆盖分叉，需要单独修：** 入口决策器对最终分数页返回 `Complete`（`LabyrinthEntryActionPlanner.kt:385-388`），正常路径会因该页在交接集合内（`:2942`）转给路线执行；若 `nodeExecutionConfigured()` 为 false（未配路线或模板），则走 `finishFromPlanner`，直接停在最终分数页。这是一个独立小 bug，列入阶段零。

## 修正二：按状态裁剪识别器只在锚点层缺失，且不是性能瓶颈

原方案第 7 节认为「同一帧运行太多无关识别器」。

**重识别器与 OCR 层已经全部按页面裁剪。** 八个重识别器各有独立页面条件（`LabyrinthEntryFrameProcessor.kt:423-498`），七个 Android OCR 解析器全部有页面守卫早返回。

**每帧无差别全跑的只有约 118 条锚点模板定义**（`LabyrinthEntryFrameProcessor.kt:569-708`），战斗中也照样跑标题页 logo、公告、通关宝箱模板。这一项确实成立。

但实测数据说明它不值得优先做：

| 页面 | 帧数 | 平均耗时 | p95 | 其中节点搜索 | 其中锚点及其他 |
|---|---|---|---|---|---|
| NODE_SELECTION | 401 | 2430ms | 8415ms | 2118ms | 125ms |
| BATTLE_TEAM_SELECTION | 1078 | 433ms | 932ms | 0 | 425ms |
| BATTLE_IN_PROGRESS | 678 | 130ms | 218ms | 0 | 121ms |
| UNKNOWN | 293 | 197ms | 308ms | 42ms | 136ms |
| SHOP | 258 | 298ms | 349ms | 0 | 约290ms |
| 其余 19 种页面 | 约460 | 115-624ms | — | 0 | — |

锚点层总开销约 120ms/帧，按阶段裁剪乐观估计省 60-80ms。节点搜索单帧 2118ms，是它的 26 倍。

**结论：原方案 Phase 2（有界识别）在 Phase 3（节点重构）之前，顺序与实际成本分布相反。修订版将两者交换。**

## 修正三：节点打分已有路线先验，缺的是节点级与时序

原方案第 12 节认为节点得分只看视觉相似度。

**列级打分已经大量融合路线先验**（`NodeTopologyMapper.chooseBestAlignment`，`:202-349`）：期望列 5.0、当前列 2.5、下一列 1.0、可达数一致 4.0、跳列惩罚 1.5、列间距过近惩罚 60.0。视觉置信度在这一层只是平局决胜项，权重 2.0。

原方案举的例子（路线说 EX、视觉 SHOP 分数更高）已经有专门处理：类型冲突被显式建模为 `NodeAction.TypeConflict`，且不允许路线单方面覆盖视觉（`NodePositionMapper.kt:42-55`）。

**确实缺失的是两项：**

- **节点级打分无先验**：非极大值抑制的排序纯视觉（`LabyrinthNodeClassifier.kt:757-769`），只看可点击性、是否激活模板、置信度、辉光覆盖率。
- **时序一致性完全没有**：没有任何跨帧候选跟踪或命中累积。唯一的时序机制是两处粗粒度门（5 帧跟踪复用、点击前 3 帧稳定）。

本项保留，但范围收窄到上述两点。

---

# 第二部分：现状清单

审计把原方案提到的每项能力归为三类。位置引用以 HEAD `1e0f0f9` 为准。

## 已完整存在（原方案不必再做）

| 能力 | 位置 |
|---|---|
| 未识别页面作为硬动作门（绝对分 0.45 加分差 0.08 双重否决） | `vision/LabyrinthEntryClassifier.kt:303-319` |
| 识别端 ROI（商店三卡、战斗按钮、连结/遗物三选一、移动确认弹窗全部固定坐标） | `LabyrinthEntryFrameProcessor.kt:569-708`、`LabyrinthShopRecognition.kt:286-309` 等 |
| 重识别器与 OCR 按页面裁剪 | `LabyrinthEntryFrameProcessor.kt:423-498`、七个 `Android*Resolver.kt` |
| 节点点击三段式提交（点击、移动确认、目标页类型校验） | `LabyrinthEntryRecognitionSession.kt:5336, 5612, 5706`、`:449-480` |
| 商店购买、商店刷新、遗物获得各自的两段式提交 | `:4563→2173`、`LabyrinthPageRuntimePolicy.kt:222-235`、`LabyrinthRelicAcquisitionGate.kt` |
| 孤儿确认与类型冲突的有条件恢复 | `LabyrinthNodeEntryRecovery.kt`、`node/LabyrinthNodeConflictRecovery.kt` |
| 节点定位的五级恢复链（保持、滚图、诊断、微调、停机） | `:5287, 5917, 6070, 5842, 7003` |
| 六阶段最终结算链 | `:564-571, 3586-3611, 4741-4752` |
| 列级路线先验与拓扑一致性打分 | `NodeTopologyMapper.kt:202-349, 511-535` |
| 类型冲突显式建模 | `NodePositionMapper.kt:42-55`、`LabyrinthNodeActionPlanner.kt:125-206` |
| 视口跟踪器雏形（世界坐标、可见列、滚动方向、边界记忆、目标位置预测） | `node/LabyrinthNodeViewportScanner.kt` |
| 滑动位移的三级几何验证（相机位移、共同节点位移、签名兜底） | `LabyrinthNodeViewportScanner.kt:88-122` |
| 角色 slot-first 识别（编组页像素投影测网格、三选一固定槽、成员条独立精修） | `LabyrinthBattleTeamRecognition.kt:539`、`LabyrinthCharacterGridDetector.kt:146`、`LabyrinthCharacterRecognition.kt:262-314` |
| 头像 UI 掩码（等级、星级、选中角标、底部进度点、视口裁切半行） | `LabyrinthCharacterIconMatcher.kt:14-22, 308-321` |
| 属性徽章作为识别先验 | `LabyrinthCharacterIconMatcher.kt:68-77` |
| Top-K 候选（最多 5 个）加名称 OCR 复核 | `LabyrinthCharacterIconMatcher.kt:122`、`AndroidLabyrinthRoleRewardNameResolver.kt`（仅三选一页） |
| 决策层的角色不重复约束（本局已有、在途已选、跨 Boss 队已用） | `:1869-1877, 2644-2647, 4593-4619` |
| 单帧网格几何（列中心、行高、上下边界、上下半行吸附） | `LabyrinthCharacterGridDetector.kt` |
| 三选一角色边际价值（候选并入当前池后做最佳部署，取部署后系统分） | `LabyrinthRoleDecisionPolicy.kt:1493-1570` |
| Boss 三队联合规划（安全容量取最小值，再最大化不相交队伍总分） | `LabyrinthRoleDecisionPolicy.kt:1708-1793` |
| 调试面板结构化历史（600 帧，含页面、置信度、分段耗时、识别框、消息） | `debug/LabyrinthDebugDashboardServer.kt:275-370` |

## 部分存在（原方案的对应项需要收窄）

| 能力 | 已有 | 缺口 |
|---|---|---|
| 流程阶段 | `entryPhaseComplete`、`LabyrinthPostBossStage`、`LabyrinthEffectiveCharacterScanStage`、`LabyrinthBossEditorPreparationStage`、`LabyrinthPageUiOwner` | 五个互不协调的私有小状态，没有统一状态机，没有"当前阶段是否允许该页面"的判断 |
| 稳定帧 | 至少七套计数器，阈值 2 到 12 不等 | 不统一；入口决策器走独立计数；除节点点击外，点击目标矩形没有跨帧稳定性要求 |
| 冷却与重试预算 | 每条派发路径各有常量 | 各处独立，无集中校验 |
| 分级恢复 | 节点定位路径完整 | 其余至少八条路径（确认超时、类型不匹配、未知页超时、预算耗尽、角色获得超时、结算超时等）全部一次性停机 |
| 视口跟踪 | 能拟合相机偏移、预测目标屏幕位置 | 每帧从全扫结果重新拟合，非增量；预测只用于"要不要滑"，从不用于缩小搜索区域；拓扑置信度低于 0.70 时整体失效退回盲扫 |
| 节点位置与类型分离 | 底座模板做提案、梯度模板做精修与分类；颜色语义独立一路 | 提案模板仍按类型分开，不存在类型无关的"节点性"检测器 |
| 头像归一化 | 掩码强 | 无 resize 到统一尺寸；内缩是固定常数 6/14 像素，不随分辨率缩放；三选一大卡与编组页掩码组合不一致 |
| Top-K | 匹配器输出 5 个候选 | 编组页在 `observeSlot` 后丢弃候选列表（`LabyrinthBattleTeamRecognition.kt:971-987`）；名称复核只覆盖三选一页 |
| 角色规则约束 | 决策层完整 | 无一条反馈回识别层；模板池永远是全角色图标包 |

## 确实缺失（修订版的工作项来源）

| 编号 | 缺口 | 位置 |
|---|---|---|
| N1 | 路线目标不参与视觉搜索，每个锚点最多 272 个偏移窗口暴力搜索 | `LabyrinthNodeClassifier.kt:537-622`、`NodeAnchorDefinitions.kt:141-150` |
| N2 | 滚动后总是全屏重扫，预测区域不用于裁剪候选 | `LabyrinthNodeClassifier.kt:559`、`LabyrinthNodeViewportScanner.kt:291-296` |
| N3 | 视口签名依赖分类结果，分类失败即跟踪器失效 | `LabyrinthEntryRecognitionSession.kt:483-496` |
| N4 | 节点级打分无先验、无时序一致性 | `LabyrinthNodeClassifier.kt:757-769` |
| S1 | 状态机转移靠中文文案字符串匹配 | `LabyrinthEntryRecognitionSession.kt:4569-4577, 4593-4699` |
| S2 | 五条独立动作派发路径，各自重写检查，无集中审批点 | `:2840, 4547, 5312, 5584, 4409` |
| S3 | 动作端约十处硬编码兜底坐标绕过 ROI | `:3821, 3858, 3870, 4060, 4069, 4094, 4103, 4518-4520` |
| S4 | 最终分数页在未配路线时直接停机 | `LabyrinthEntryActionPlanner.kt:385` + `:2820-2835` |
| S5 | 死代码 `LabyrinthScreenClassifier` / `LabyrinthScreenStage` | `vision/LabyrinthScreenClassifier.kt` |
| S6 | 7045 行单类，单函数 720 行 | `LabyrinthEntryRecognitionSession.kt` |
| C1 | 无跨槽位一对一约束求解，逐槽独立加事后拒绝重复 | `LabyrinthBattleTeamSelectionPlan.kt:586-603`、`LabyrinthRoleRewardChoicePlanner.kt:85-87` |
| C2 | 无跨帧角色网格状态（滚动偏移、全局行索引） | `LabyrinthBattleTeamRecognition.kt:580` |
| C3 | 几何逻辑分散（成员条、三选一、Boss 特判、滑动手势三处重复） | 见审计 |
| T1 | 普通战与 Boss/EX 用同一目标函数，没有"最低成本可过"与后续机会成本 | `LabyrinthRoleDecisionPolicy.kt` 全文无 opportunity cost 项 |
| T2 | 商店买角色不走角色评分器 | `LabyrinthShopPolicy.kt` 只买遗物 |
| R1 | 历史记录缺审批决策字段（期望页面、候选、拒绝原因） | `LabyrinthDebugDashboardServer.kt:275-370` |
| R2 | 无离线回放入口 | 无 `ReplaySession` |

---

# 第三部分：修订后的实施顺序

原方案六个阶段按「壳、有界识别、节点、角色、配队、回放」排列。修订版按「收益除以风险」重排，并把回放提到最前。

```text
阶段零   回放基础与小修                 低风险，是后面每一步的验证前提
阶段一   节点定向搜索                   唯一的性能问题，收益最大
阶段二   状态机去文案化                 纯局部重构，消除最脆弱的一环
阶段三   审批壳（收敛而非叠加）         大重构，必须合并五条派发路径
阶段四   角色 Top-K 透传与一对一约束    中等，局部可验证
阶段五   有界锚点识别                   小收益，放最后
降级     头像 resize 归一化、网格几何大统一、普通战机会成本
```

每个阶段独立可合并，不依赖后续阶段。

---

## 阶段零：回放基础与小修

目标：让后面每个阶段都能用同一份历史数据对比前后行为，而不是重新打游戏。

### 0.1 扩展历史记录字段

调试面板已有 600 帧结构化历史（`LabyrinthDebugDashboardServer.kt:275-370`）。在现有 JSON 上追加，不改现有字段：

```text
expectedPages          当前阶段允许的页面集合（阶段三之前先填全集）
actionLabel            本帧派发的动作文案，无则空
actionRejectReason     本帧拒绝动作的原因，无则空
nodeTarget             当前路线目标 blockId
nodeSearchWindowCount  本帧节点搜索实际评估的候选窗口数
nodeTrackerState       视口跟踪器是否可靠、拟合偏移、方向
```

前两项为阶段三预留，先写占位值。后三项是阶段一的度量基线。

### 0.2 离线回放入口

新增测试工具类 `LabyrinthReplayHarness`：读取 `history.ndjson` 加对应帧（当前只有最后一帧 JPEG，回放先用已有的 16 张 fixture 截图），跑 `LabyrinthEntryFrameProcessor.process`，输出：

```text
totalFrames
unknownFrames
avgRecognitionMs / p95RecognitionMs
nodeSearchWindowCount 分布
```

不做动作，不依赖 Android 运行时。放在 `app/src/test/`，与现有 `LabyrinthNodeScreenshotBatchDiagnosticTest` 同级。

### 0.3 调试包补充逐帧截图

现在调试包只带最后一帧 JPEG（`JPEG_REFRESH_INTERVAL_MILLIS = 750`）。回放需要逐帧图像。增加一个开关：开启后每帧写一张缩放到 960 宽的 JPEG 到应用私有目录，按帧号命名，600 帧约 30 MB，与历史记录一起打包。默认关闭。

### 0.4 三处小修

- S4：`nodeExecutionConfigured()` 为 false 时最终分数页不再停机，改为等待或提示，不做点击。
- S5：删除 `LabyrinthScreenClassifier` 与 `LabyrinthScreenStage`。
- S3 的一半：把约十处硬编码兜底坐标收拢为一张常量表（页面到兜底点），加单元测试断言每个点都落在该页面已声明的 ROI 内。不改行为，只让阶段三能引用。

验收：新字段出现在调试包里；回放工具能对 16 张 fixture 输出统计；单元测试全绿。

---

## 阶段一：节点定向搜索

目标：把 NODE_SELECTION 页面平均 2430ms 降到 500ms 以下，p95 从 8415ms 降到 1500ms 以下。

### 1.1 现状

`classifyMapNodes`（`LabyrinthNodeClassifier.kt:537-662`）对 16 个列锚点各做最多 272 个偏移窗口的模板提案，路线目标完全不参与。视口跟踪器（`LabyrinthNodeViewportScanner`）已经能预测目标列的屏幕中心（`expectedScreenCenterX`，`:291`），但预测只用来决定要不要滑动。

### 1.2 改动

**新增"目标窗口"输入。** `classifyMapNodes` 增加可选参数：

```kotlin
data class NodeSearchHint(
    val targetBlockId: Long,
    val expectedCenterX: Int?,      // 来自 viewportScanner，可为空
    val expectedTypes: Set<Int>,    // 目标类型加可达后继类型
    val reachableColumns: IntRange, // 逻辑列范围
)
```

由 `LabyrinthEntryFrameProcessor` 从 session 注入。这确实打破了"帧处理器纯视觉"的分层，但只加一个可空参数，为空时行为与现在完全一致。

**三级搜索。**

```text
L1 定向：expectedCenterX 非空时，只在该 x 附近正负一个列距内的锚点、
        正负 3 个偏移步的窗口里搜索，模板只用 expectedTypes。
        预计窗口数从 16 × 272 降到约 3 × 49。
L2 扩大：L1 连续 2 帧找不到目标，扩大到全部锚点，模板仍限 expectedTypes。
L3 全扫：L2 再 2 帧失败，退回现有全扫。
```

搜索级别写进 `lastSearchMode`（现在只有 tracked 和 full），历史记录里可见。

**跟踪器改为增量优先。** `observe()`（`LabyrinthNodeViewportScanner.kt:124`）在拓扑置信度低于 0.70 时不再整体失效，改为：上一帧几何可靠时，用共同 blockId 的中心位移中位数增量更新 `viewportWorldLeft`，标记为"推算"；连续 3 帧推算后才降级为不可靠。

**视口签名与分类解耦。** `labyrinthNodeViewportSignature`（`LabyrinthEntryRecognitionSession.kt:483-496`）现在由分类结果拼成，分类失败签名为空。改为复用 `LabyrinthNodeClassifier.viewportSignature`（`:665`）的像素哈希，让边界判断在分类失败时仍有依据。

### 1.3 不做的事

- 不做类型无关的"节点性"检测器。现有底座模板提案已经是弱形式的位置检测，收益不明确。
- 不改 272 个偏移的暴力表本身，只改进入它的条件。
- 不做时序一致性的候选跟踪。L1 的"连续 2 帧"已是最低限度的时序门，够用。

### 1.4 验收

- 阶段零回放工具在 16 张 fixture 上，L1 命中率不低于 80%，且 L1 命中的目标与 L3 全扫结果一致。
- 节点相关测试（`LabyrinthNodePositionTest` 1990 行、`LabyrinthNodeClassifierFixtureTest` 436 行、`LabyrinthNodeViewportScannerTest` 208 行）全部通过，不允许改断言，只允许新增。
- 实机一局完整路线，历史记录里 NODE_SELECTION 平均耗时低于 500ms。

### 1.5 风险

这一层的测试有 7469 行截图断言。改动会让部分 fixture 的搜索路径从 full 变为 directed，结果应当一致，但任何一处不一致都要人工核对截图，不能改断言凑数。

---

## 阶段二：状态机去文案化

目标：提交逻辑与阶段推进不再依赖中文字符串匹配。

### 2.1 现状

`dispatchPostEntryTap` 的 Executed 分支（`LabyrinthEntryRecognitionSession.kt:4569-4577, 4593-4699`）用文案前缀和整串匹配决定：事件、遗物、角色三选一的提交标志；最终结算的阶段推进；商店购买上下文；单队 Boss 切多队。改一句 UI 文案会静默破坏整条链。

### 2.2 改动

新增：

```kotlin
internal enum class LabyrinthPostEntryActionKind {
    SELECT_EVENT, ADVANCE_EVENT,
    SELECT_RELIC, SELECT_ROLE_REWARD, EVENT_FREE_ROLE_SELECT, EVENT_FREE_ROLE_CONFIRM,
    CLOSE_FINAL_SCORE, CLOSE_FINAL_ITEM,
    SHOP_BUY, SHOP_CLOSE_PURCHASE_COMPLETE, SHOP_REFRESH,
    BATTLE_RETRY, BATTLE_RETRY_SWITCH_MULTI,
    ADVANCE_CHARACTER_ACQUISITION, COLLAPSE_PORTRAIT,
    EX_PROBE_DETAIL, EX_CLOSE_DETAIL,
    GENERIC,
}
```

`dispatchPostEntryTap` 增加 `kind` 参数，Executed 分支改为按 `kind` 分派。文案只用于显示。约 300 行的页面计划表（`:3810-4111`）每个分支返回 `Triple(kind, label, rect)`。

### 2.3 验收

- 把所有文案改成随机字符串跑一遍单元测试，行为不变。
- 会话文件中不再存在对 `label` 的相等或前缀比较。

---

## 阶段三：审批壳（收敛而非叠加）

目标：五条动作派发路径合并到一个校验点；引入阶段状态机；非法页面绝不触发动作。

### 3.1 前提

这是六个阶段里唯一的大重构，承载它的是 7045 行的单类。**如果只是在现有五条路径前再加一层门禁，会得到第六套并行门禁，净增技术债。** 阶段三的定义是收敛，不是叠加。

### 3.2 改动

**阶段枚举。** 与原方案第 4.1 节一致，但把现有五个私有小状态映射进去而不是并存：

```text
FlowPhase                          吸收
ENTRY_*                            entryPhaseComplete、入口决策器的内部状态
MAP_IDLE / MAP_NODE_CLICKED /      pendingNodeTransition 的三段
MAP_MOVE_CONFIRM
NODE_CONTENT                       combatContext、activeNodeType
NODE_REWARD_LOOP                   roleRewardBatchActive、characterAcquisitionActive
TEAM_EDITOR                        LabyrinthBossEditorPreparationStage、
                                   LabyrinthEffectiveCharacterScanStage
RUN_FINAL_SETTLEMENT               LabyrinthPostBossStage
```

**审批表。** 每个 FlowPhase 声明 `allowedPages`、`allowedActionKinds`（用阶段二的枚举）、`timeoutMillis`、`onTimeout`。作为纯数据放在独立文件，可单测。

**单一派发点。** 新增 `ActionGate.approve(proposal): ApprovalResult`，输入是 `(phase, page, actionKind, targetRect, stableFrames, lastActionAt, attempts)`，输出批准或拒绝原因。五条派发路径（`:2840, 4547, 5312, 5584, 4409`）改为先构造 proposal 过 gate，再执行。现有的七套稳定帧计数器、各处冷却常量、各处预算常量收敛到 gate 的输入。

**兜底坐标进 ROI 校验。** 阶段零收拢的常量表在这里接入：gate 拒绝任何不在当前页面声明 ROI 内的 targetRect。

**其余路径的分级恢复。** 原方案第 33 至 34 节要求的统一恢复链，在这里以审批表的 `onTimeout` 实现：先重试识别、再扩大候选、再暂停，替代现有八条路径的一次性停机。

**审批日志。** 每次 approve 调用写一条到阶段零预留的 `actionLabel` / `actionRejectReason` 字段。

### 3.3 分步落地

不能一次合并五条路径。顺序：

```text
3a  只加 FlowPhase 与审批表，gate 只记录不拒绝（影子模式），跑三局对比日志
3b  dispatchPostEntryTap 接入 gate（最主要的一条）
3c  节点点击与移动确认接入
3d  编组动作与入口动作接入
3e  删除各路径原有的稳定帧、冷却、预算局部变量
```

每一步独立提交，独立实机验证。

### 3.4 验收

原方案第 40 节第一版验收目标的 1、2、3、4、7、8 条（第 5 条奖励循环已存在，第 6 条已存在）。补充：

- 会话文件里稳定帧相关字段只剩一个。
- 影子模式三局日志里，gate 拒绝而旧逻辑放行的动作，每一条都人工确认是误动作或等价。

---

## 阶段四：角色 Top-K 透传与一对一约束

目标：编组页不再因同一角色被识别到两个槽而放弃。

### 4.1 改动

- `observeSlot`（`LabyrinthBattleTeamRecognition.kt:971-987`）把 `candidates` 透传到 `LabyrinthBattleCharacterMatch`。
- 新增 `assignSlotsOneToOne(slots: List<Top-K>)`：贪心按最高分逐槽分配，已分配的角色从其他槽候选里剔除，冲突时取次优。不用匈牙利算法，五个槽的贪心足够。
- `LabyrinthBattleTeamSelectionPlanner` 用分配结果代替按置信度取最大值。
- 三选一的"发现重复就放弃"（`LabyrinthRoleRewardChoicePlanner.kt:85-87`）改为先走同一分配函数，分配后仍重复才放弃。

### 4.2 不做的事

- 不把"本局已有角色"反馈进识别层的模板池。改模板池会改变所有分数分布，需要重标三处门槛。
- 不做头像 resize 归一化，理由同上。

### 4.3 验收

现有 `LabyrinthBattleTeamRecognitionTest`、`LabyrinthBattleTeamSelectionPlanTest` 全绿；新增两个测试覆盖"两槽同角色"和"三选一重复候选经分配后消歧"。

---

## 阶段五：有界锚点识别

目标：把每帧约 118 条锚点定义按 FlowPhase 裁剪。

依赖阶段三的 FlowPhase。每个阶段声明 `anchorIds`，`LabyrinthEntryFrameProcessor.process` 增加可选的 `allowedAnchorIds` 参数。连续 3 帧全部低于阈值时扩大到全集（对应原方案的 Recovery Path），扩大后连续 5 秒仍未知才是原方案的 Diagnostic Scan。

预期收益每帧 60 到 80ms，放最后。

---

## 降级项（本轮不做）

| 项目 | 理由 |
|---|---|
| 头像 resize 到统一尺寸 | 改变所有角色的绝对相似度分布，三处独立门槛（0.35/0.045、0.50/0.10、0.045）需重标，现有截图回归期望分数全部失效；当前角色识别没有对应的失败率数据支持 |
| 角色网格几何大统一 | 涉及三套布局配置、827 行网格检测器、独立成员条检测器；槽位标识从可见坐标改全局坐标会牵动执行步骤去重和搜索签名；近期滚动问题已由 `roster-probe-scroll.md` 的方案解决 |
| 普通战最低成本队伍与机会成本 | 需要先定义"本局后续 Boss/EX 需要谁"的估计模型，现在没有数据；先让阶段零回放积累一批完整局的角色使用记录 |
| 商店角色评分器 | 商店目前只买遗物（`LabyrinthShopPolicy.kt`），扩展到角色是新功能不是优化 |
| 类型无关的节点位置检测器 | 阶段一的定向搜索已把窗口数压到原来的约 3%，再分离检测器收益不明确 |
| 节点级时序跟踪 | 阶段一 L1 的两帧确认已够用 |

---

# 第四部分：验收目标（修订版）

## 阶段零完成时

```text
1. 调试包含逐帧截图与新增字段
2. 回放工具能对 fixture 输出耗时与窗口数统计
3. 未配路线时最终分数页不停机
```

## 阶段一完成时

```text
1. NODE_SELECTION 平均耗时低于 500ms，p95 低于 1500ms
2. 历史记录里 lastSearchMode 以 directed 为主
3. 分类失败时视口跟踪器仍能判断边界
4. 节点测试 7469 行零断言修改
```

## 阶段二完成时

```text
1. 会话文件里不再有 label 字符串比较
2. 文案随机化后单元测试行为不变
```

## 阶段三完成时

```text
1. 非法页面绝不触发动作，拒绝原因可在日志查到
2. 入口闪过节点地图不会提前进入路线状态机
3. 未知页只在允许的阶段做受限点击
4. 兜底坐标全部经过 ROI 校验
5. 稳定帧、冷却、预算各只剩一处定义
6. 影子模式三局无未解释的拒绝差异
```

## 阶段四完成时

```text
1. 两槽同角色经分配后各自消歧
2. 三选一重复候选不再直接放弃
```

---

# 第五部分：与原方案的对照

| 原方案 | 修订版处理 |
|---|---|
| Phase 1 审批壳 | 移到阶段三，前置阶段二去文案化，改为收敛式，分五步影子上线 |
| Phase 2 有界识别 | 移到阶段五，范围收窄到锚点层 |
| Phase 3 节点识别重构 | 提到阶段一，范围收窄到定向搜索加增量跟踪，不做检测器分离 |
| Phase 4 角色识别重构 | 收窄为阶段四的 Top-K 透传与一对一分配；归一化与网格统一降级 |
| Phase 5 配队决策重构 | 边际价值与 Boss 联合规划已存在；机会成本降级待数据 |
| Phase 6 回放系统 | 提到阶段零 |
| 第 31 节 RUN_CLEAR_RESULT 修复 | 已存在，改为修一条未覆盖分叉 |
| 第 33 至 34 节 Recovery 机制 | 节点路径已存在；其余路径的分级恢复并入阶段三审批表的 `onTimeout` |
| 第 39 节不建议同时动的东西 | 保留，并追加头像归一化与网格统一 |
| 第 43 节最终设计原则 | 保留 |

原方案第 44 节的一句话总结仍然成立：让识别器没有机会在不该做决定的时候做决定。修订版只是把「先做哪一件」按实测数据重排了。
